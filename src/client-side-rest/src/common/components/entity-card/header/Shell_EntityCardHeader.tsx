import { Avatar, CardHeader } from '@mui/material';

export const Shell_EntityCardHeader = () => {
  return <CardHeader
    title={'...'}
    subheader={'...'}
    avatar={<Avatar aria-label='recipe'>...</Avatar>}
    action={''}
  />;
}